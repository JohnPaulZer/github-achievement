# Frontend

This is the React, TypeScript, Vite, and Tailwind frontend for GitHub Achievement Counter.

For full project setup, token instructions, production notes, API endpoints, and app usage, see the root documentation:

```text
../README.md
```

Common frontend commands:

```bash
npm install
npm run dev
npm run build
npm run preview
```
